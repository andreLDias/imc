import React from 'react';
import { render, screen } from '@testing-library/react';
import MainCalculator from './index';

describe('[Test case] MainCalculator', () => {
  it.todo('Should match snapshot')
  it.todo('Should render all components')
  it.todo('Button should start disabled')
  it.todo('Button should be enabled when fields are filled')
})
